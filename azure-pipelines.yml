# CICD
trigger:
- main

variables:
  dockerHubNamespace: shawntzx
  imageName: spamclassifier_app
  azureServiceConnection: AzureAppServiceConnection
  appServiceName: my-spamclassifier-app  # Replace with your actual App Service name

pool:
  name: Default
  demands:
    - agent.name -equals DESKTOP-6I5OS4B # Replace with your agent name

stages:
- stage: Build
  jobs:
  - job: BuildAndPush
    displayName: Build and Push Docker Image
    pool:
      name: Default
    steps:
    - task: Docker@2
      inputs:
        containerRegistry: 'DockerHubConnection'
        repository: '$(dockerHubNamespace)/$(imageName)'
        command: 'buildAndPush'
        Dockerfile: '**/docker/Dockerfile'
        tags: |
          $(Build.BuildId)

- stage: Deploy
  dependsOn: Build
  jobs:
  - job: DeployToAzure
    displayName: Deploy to Azure App Service
    pool:
      name: Default
    steps:
    - task: AzureWebAppContainer@1
      inputs:
        azureSubscription: $(azureServiceConnection)
        appName: $(appServiceName)
        imageName: '$(dockerHubNamespace)/$(imageName):$(Build.BuildId)'
        startupCommand: ''
